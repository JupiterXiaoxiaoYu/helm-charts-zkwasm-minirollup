apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-merkleservice
spec:
  selector:
    matchLabels:
      app: merkleservice
  template:
    metadata:
      labels:
        app: merkleservice
    spec:
      containers:
      - name: merkleservice
        image: "{{ .Values.merkleservice.image.repository }}:{{ .Values.merkleservice.image.tag }}"
        command: ["./target/release/csm_service", "--uri", "{{ .Values.merkleservice.mongodbUri }}"]
        ports:
        - containerPort: {{ .Values.merkleservice.port }}
        env:
        - name: URI
          value: {{ .Values.merkleservice.mongodbUri }}